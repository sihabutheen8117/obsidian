- it is a state container for js apps.
- commonly used with react.
- it is library for js app.

- it store the state and manage the state of the js app 
- toolkit is used to write a code in standard way.
- redux has a many boiler plate code (rewrite ) ,so we used redux toolkit.

- through react-redux package we can use redux in react.