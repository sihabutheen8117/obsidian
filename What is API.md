
### API - application user interface

##### So what is interface
- yeah, interface is the abstraction way of delevering the content to the user
- it is simplify , content into user readable form.
- it abstract , more complex term in behind that.
- so user can see only , what they want , simply it make the thing easier to the user.

##### Then what is API
- api is the abstraction of the thing
- for example if we want to change the color of the image into black and white , we can simply download the package , and simply convert them into b and w ,
- we dont need to look at what happen behind them.

##### In web API
- in web api same thing they doing.
- they make a api for the developer , who want to use theire product in the abstract manner , so developer no need to thing , how they works ,simply make the api call , then they provide their abstract data to them.