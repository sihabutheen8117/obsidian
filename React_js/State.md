
state is owned by the component. this.state is private to the component and as we’ll see can be updated with this.setState().

**when the state or props of a component update, the component will re-render itself.**

